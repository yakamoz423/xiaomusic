#!/command/with-contenv bashio
# shellcheck shell=bash
if [[ "${1}" -ne 0 ]] && [[ "${1}" -ne 256 ]]; then
  bashio::log.warning "XiaoMusic UI crashed with exit ${1}"
else
  bashio::log.info "XiaoMusic UI stopped"
fi
